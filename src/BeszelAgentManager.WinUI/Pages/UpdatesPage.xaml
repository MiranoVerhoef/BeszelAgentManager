<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="BeszelAgentManager.WinUI.Pages.UpdatesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="24,16,24,24" RowSpacing="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Style="{StaticResource TitleTextBlockStyle}" Text="Updates" />

        <StackPanel Grid.Row="1" Orientation="Horizontal" Spacing="12">
            <Button x:Name="CheckManagerButton" Click="CheckManagerButton_Click" Content="Check manager" />
            <CheckBox x:Name="IncludePrereleasesCheckBox" Content="Include prereleases" />
        </StackPanel>

        <InfoBar
            x:Name="ManagerUpdateInfoBar"
            Grid.Row="2"
            IsOpen="True"
            IsClosable="False"
            Severity="Informational"
            Title="Ready"
            Message="Check GitHub for the latest manager release." />

        <TextBox
            x:Name="ReleaseNotesTextBox"
            Grid.Row="3"
            AcceptsReturn="True"
            IsReadOnly="True"
            TextWrapping="Wrap"
            Header="Release notes"
            PlaceholderText="Release notes will appear here after checking for updates." />
    </Grid>
</Page>
