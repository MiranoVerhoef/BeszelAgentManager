<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="BeszelAgentManager.WinUI.Pages.ExtraPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="12" RowSpacing="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border Padding="14" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="4">
            <StackPanel Spacing="8">
                <TextBlock FontWeight="SemiBold" Text="GitHub Authentication" />
                <TextBlock Foreground="{ThemeResource TextFillColorSecondaryBrush}" Text="Set a GitHub token to avoid API rate limiting. Token is stored encrypted (DPAPI)." />
                <TextBlock Foreground="{ThemeResource TextFillColorSecondaryBrush}" Text="Tip: If GITHUB_TOKEN is set as an environment variable, it overrides the saved token." />
                <Button x:Name="GitHubAuthButton" Width="170" HorizontalAlignment="Left" Content="GitHub Auth..." Click="GitHubAuthButton_Click" />
            </StackPanel>
        </Border>

        <Border Grid.Row="1" Padding="14" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="4">
            <StackPanel Spacing="8">
                <TextBlock FontWeight="SemiBold" Text="Agent Fingerprint" />
                <TextBlock Foreground="{ThemeResource TextFillColorSecondaryBrush}" Text="View or reset the agent fingerprint (beszel-agent fingerprint view/reset)." />
                <TextBlock Foreground="{ThemeResource TextFillColorSecondaryBrush}" Text="Reset is useful if you cloned a VM or want to re-register this machine." />
                <StackPanel Orientation="Horizontal" Spacing="10">
                    <Button x:Name="ViewFingerprintButton" Width="170" Content="View fingerprint" Click="ViewFingerprintButton_Click" />
                    <Button x:Name="ResetFingerprintButton" Width="170" Content="Reset fingerprint" Click="ResetFingerprintButton_Click" />
                </StackPanel>
            </StackPanel>
        </Border>

        <Border Grid.Row="2" Padding="14" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="4">
            <StackPanel Spacing="8">
                <TextBlock FontWeight="SemiBold" Text="Periodic service restart" />
                <TextBlock Foreground="{ThemeResource TextFillColorSecondaryBrush}" Text="Optionally restart the Beszel Agent service on a fixed interval." />
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <CheckBox x:Name="AutoRestartCheckBox" Content="Restart Beszel Agent every" Click="AutoRestartCheckBox_Click" />
                    <NumberBox x:Name="RestartIntervalNumberBox" Width="72" Minimum="1" Maximum="168" Value="24" SpinButtonPlacementMode="Compact" ValueChanged="RestartIntervalNumberBox_ValueChanged" />
                    <TextBlock VerticalAlignment="Center" Text="hour(s)" />
                </StackPanel>
            </StackPanel>
        </Border>

    </Grid>
</Page>
