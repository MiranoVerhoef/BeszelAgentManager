<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="BeszelAgentManager.WinUI.Pages.LogsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="12,10,12,8" RowSpacing="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border Padding="12,10" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="4">
        <Grid ColumnSpacing="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="180" />
            </Grid.ColumnDefinitions>
            <CheckBox x:Name="DebugLoggingCheckBox" Content="Enable Manager Debug Logging" Click="DebugLoggingCheckBox_Click" />
            <Button x:Name="ExportSupportBundleButton" Grid.Column="1" Content="Export Support Bundle" Click="ExportSupportBundleButton_Click" />
        </Grid>
        </Border>

        <Rectangle Grid.Row="1" Height="1" Fill="{ThemeResource DividerStrokeColorDefaultBrush}" />

        <StackPanel Grid.Row="2" Spacing="2">
            <TextBlock x:Name="ManagerLogFolderText" Text="Manager log folder: C:\ProgramData\BeszelAgentManager" />
            <TextBlock x:Name="ManagerLogFileText" Foreground="{ThemeResource TextFillColorSecondaryBrush}" Text="Current file: C:\ProgramData\BeszelAgentManager\manager.log" />
        </StackPanel>

        <Border Grid.Row="3" Padding="12,10" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="4">
        <Grid RowSpacing="8">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Grid ColumnSpacing="8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="120" />
                    <ColumnDefinition Width="92" />
                    <ColumnDefinition Width="92" />
                    <ColumnDefinition Width="100" />
                </Grid.ColumnDefinitions>
                <TextBlock VerticalAlignment="Center" Text="File:" />
                <ComboBox Grid.Column="1" x:Name="LogSelector" SelectionChanged="LogSelector_SelectionChanged" />
                <TextBlock Grid.Column="2" VerticalAlignment="Center" Text="Type:" />
                <ComboBox Grid.Column="3" x:Name="TypeFilterComboBox" SelectionChanged="TypeFilterComboBox_SelectionChanged" SelectedIndex="0">
                    <ComboBoxItem Content="All" />
                    <ComboBoxItem Content="Info" />
                    <ComboBoxItem Content="Warning" />
                    <ComboBoxItem Content="Error" />
                    <ComboBoxItem Content="Debug" />
                    <ComboBoxItem Content="Task" />
                </ComboBox>
                <Button Grid.Column="4" x:Name="RefreshButton" Click="RefreshButton_Click" Content="Refresh" />
                <Button Grid.Column="5" Content="Rotate now" Click="RotateNowButton_Click" />
                <Button Grid.Column="6" Content="Open folder" Click="OpenFolderButton_Click" />
            </Grid>

            <ScrollViewer
                x:Name="LogScrollViewer"
                Grid.Row="1"
                HorizontalScrollBarVisibility="Auto"
                VerticalScrollBarVisibility="Auto">
                <RichTextBlock
                    x:Name="LogTextBlock"
                    FontFamily="Consolas"
                    FontSize="12"
                    IsTextSelectionEnabled="True"
                    TextWrapping="NoWrap" />
            </ScrollViewer>
        </Grid>
        </Border>
    </Grid>
</Page>
