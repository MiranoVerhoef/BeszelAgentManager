<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="BeszelAgentManager.WinUI.Pages.EnvironmentPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="8" RowSpacing="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Margin="2,0" FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}" Text="Environment variables are always enabled." />

        <Border Grid.Row="1" Padding="10,8" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="6">
            <Grid ColumnSpacing="8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="190" />
                    <ColumnDefinition Width="440" />
                    <ColumnDefinition Width="88" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <TextBlock VerticalAlignment="Center" FontWeight="SemiBold" Text="Add environment" />
                <Button
                    x:Name="SelectEnvironmentButton"
                    Grid.Column="1"
                    MinHeight="28"
                    HorizontalAlignment="Stretch"
                    HorizontalContentAlignment="Stretch"
                    Click="SelectEnvironmentButton_Click">
                    <Grid ColumnSpacing="8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBlock
                            x:Name="SelectedEnvironmentNameText"
                            VerticalAlignment="Center"
                            FontWeight="SemiBold"
                            TextTrimming="CharacterEllipsis" />
                        <FontIcon
                            Grid.Column="1"
                            FontSize="12"
                            Glyph="&#xE70D;" />
                    </Grid>
                </Button>
                <Button Grid.Column="2" MinHeight="28" Padding="10,3" Content="Add" Click="AddEnvironmentButton_Click" />
                <TextBox
                    x:Name="EnvironmentSearchBox"
                    Grid.Column="3"
                    MinHeight="28"
                    VerticalContentAlignment="Center"
                    PlaceholderText="Search name or description"
                    TextChanged="EnvironmentSearchBox_TextChanged" />
            </Grid>
        </Border>

        <Border Grid.Row="2" Padding="10,8" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="6">
            <ListView Grid.Row="2" x:Name="EnvironmentListView" BorderThickness="0" SelectionMode="None" />
        </Border>
    </Grid>
</Page>
